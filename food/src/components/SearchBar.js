import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const SearchScreSearchBaren = () => {
  return (
    <View>
      <Text>Search Bar</Text>
    </View>
  )
}

const styles = StyleSheet.create({

})

export default SearchBar;